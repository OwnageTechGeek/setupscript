#/bin/bash

    $FILEDIR='cat /srv/minecraft/configuration/filedir
    
    #Now that the filesystem is setup we can now get to work setting up INIT and other routines.
    sh $FILEDIR/msc log 0 "Filesystem prepared, setting up INIT and other routines." 1
    cp 
